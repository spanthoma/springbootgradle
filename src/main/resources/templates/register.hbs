<!DOCTYPE HTML>

<html>
    <head>

        <link href="/webjars/bootstrap/3.3.7/dist/css/bootstrap.min.css" rel="stylesheet" />
        <script src="/webjars/bootstrap/3.3.7/dist/js/bootstrap.min.js"></script>
        <script src="/webjars/jquery/3.2.1/dist/jquery.min.js"></script>
    </head>

    <body>
        <div class="container-fluid">
            <section class="container">
                <div class="container-page">
                    <form action="/register" method="post">
                        <div class="col-md-6">
                            <h3 class="dark-grey">Registration</h3>


                            <div class="form-group col-lg-12">
                                <label>email</label>
                                <input type="text" name="username" class="form-control" id="username" value="{{user.username}}">

                                {{#if username}}
                                    <h4><span class="label label-danger">{{username}}</span></h4>
                                {{/if}}
                            </div>

                            <div class="form-group col-lg-12">
                                <label>nick</label>
                                <input type="text" name="name" class="form-control" id="name" value="{{user.name}}">
                                {{#if name}}
                                    <h4><span class="label label-danger">{{name}}</span></h4>
                                {{/if}}
                            </div>


                            <div class="form-group col-lg-6">
                                <label>Password</label>
                                <input type="password" name="password" class="form-control" id="password">
                                {{#if password}}
                                    <h4><span class="label label-danger">{{password}}</span></h4>
                                {{/if}}
                            </div>

                            <div class="form-group col-lg-6">
                                <label>Repeat Password</label>
                                <input type="password" name="confirmPassword" class="form-control" id="confirmPassword" >
                                {{#if confirmPassword}}
                                    <h4><spanel class="label label-danger">{{confirmPassword}}</spanel></h4>
                                {{/if}}
                            </div>

                        </div>

                        <div class="col-md-6">
                            <h3 class="dark-grey">Terms and Conditions</h3>

                            <p>
                                By clicking on "Register" you agree to The Company's' Terms and Conditions
                            </p>

                            <p>
                                While rare, prices are subject to change based on exchange rate fluctuations -
                                should such a fluctuation happen, we may request an additional payment. You have the option to
                                request a full refund or to pay the new price. (Paragraph 13.5.8)
                            </p>

                            <p>
                                Should there be an error in the description or pricing of a product, we will provide you with a
                                full
                                refund (Paragraph 13.5.6)
                            </p>

                            <p>
                                Acceptance of an order by us is dependent on our suppliers ability to provide the product.
                                (Paragraph 13.5.6)
                            </p>

                            <button type="submit" class="btn btn-primary">Register</button>
                        </div>
                    </form>
                </div>
            </section>
        </div>
    </body>
</html>